{
  "name": "cdata-interview-server",
  "version": "1.0.0",
  "description": "Run `npm install` if not already done.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node server.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^5.0.3",
    "express": "^5.1.0",
    "ts-node": "^10.9.2"
  }
}
